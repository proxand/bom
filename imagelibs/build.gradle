apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

android {
    namespace("com.example.image")
    compileSdkVersion 35

    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 35

    }

}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

